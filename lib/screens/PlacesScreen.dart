import 'package:flutter/material.dart';

class PlacesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lista de Lugares'),
      ),
      body: Center(
        child: Text('Pantalla de Lista de Lugares'),
      ),
    );
  }
}